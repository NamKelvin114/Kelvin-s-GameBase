using Kelvin;
using UnityEngine;

public class Popup : CacheGameComponent<Canvas>
{
    // Start is called before the first frame update
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
    }
}